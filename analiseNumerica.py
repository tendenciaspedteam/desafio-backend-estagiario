class AnaliseNumerica:
    def __init__(self, array: list[int] = None) -> None:
        """
        Inicializa a instância da classe AnaliseNumerica.
        """
        if array is None:
            array = []
        # Inicialize sua estrutura de dados aqui e mantenha-a ordenada.

    def addNum(self, num: int) -> None:
        """
        Adiciona um número ao conjunto, mantendo-o ordenado.
        """

    def findMedian(self) -> float | None:
        """
        Retorna a mediana do conjunto de números.
        """
